{
  "cron": [
    {
      "command": "bundle exec rake jobs:check_statuses",
      "schedule": "*/10 * * * *"
    },
    {
      "command": "bundle exec rake jobs:sync_registries",
      "schedule": "0 1 * * *"
    }
  ]
} 